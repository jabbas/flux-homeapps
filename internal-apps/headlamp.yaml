---
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: headlamp
  namespace: internal-apps
spec:
  interval: 24h
  url: https://kubernetes-sigs.github.io/headlamp/

---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: headlamp
  namespace: internal-apps
spec:
  chart:
    spec:
      chart: headlamp
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: headlamp
  interval: 1m0s
  values:
    ingress:
      enabled: true
      ingressClassName: traefik-internal
      hosts:
        - host: headlamp.dev.home
          paths:
            - path: /
              type: ImplementationSpecific
    config:
      pluginsDir: /build/plugins
      extraArgs:
        - -oidc-ca-file=/etc/ssl/certs/jabbas-ca.crt
      oidc:
        secret:
          create: false
        externalSecret:
          enabled: true
          name: oidc-secret-headlamp
    initContainers:
      - command:
          - /bin/sh
          - -c
          - mkdir -p /build/plugins && cp -r /plugins/* /build/plugins && chown -R 100:101 /build

        image: ghcr.io/headlamp-k8s/headlamp-plugin-flux:latest
        imagePullPolicy: Always
        name: headlamp-plugins
        securityContext:
          runAsNonRoot: false
          privileged: false
          runAsUser: 0
          runAsGroup: 0
        volumeMounts:
          - mountPath: /build/plugins
            name: headlamp-plugins
    env:
      - name: SSL_CERT_FILE
        value: /etc/ssl/certs/jabbas-ca.crt
    volumeMounts:
      - mountPath: /build/plugins
        name: headlamp-plugins
      - mountPath: /etc/ssl/certs/jabbas-ca.crt
        name: jabbas-ca
        subPath: ca.crt
        readOnly: true
    volumes:
      - name: headlamp-plugins
        emptyDir: {}
      - name: jabbas-ca
        secret:
          secretName: jabbas-ca
